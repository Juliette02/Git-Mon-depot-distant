<form method="post" class="p-0" action="{{ path('app_disc_delete', {'id': disc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disc.id) }}">
    <button class="btn btn-secondary mt-2 mb-2  col-1.5 col-lg-2 w-5">Delete</button>
</form>
